{
  "name": "twlib",
  "version": "0.0.1",
  "description": "TaskWarrior Node library interface.",
  "main": "./lib/twlib.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alan Young",
  "license": "MIT",
  "dependencies": {
    "which": ">=1.0.0"
  }
}
